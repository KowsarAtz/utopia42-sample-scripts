console.log("Running CageBuilder");

let pos = Inputs.playerPosition;
console.log("Player position: " + pos);

let x = Math.round(pos.x);
let y = Math.round(pos.y);
let z = Math.round(pos.z);

// for (let zz = z; zz < z + 20; zz++) {

// }
